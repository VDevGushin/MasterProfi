# Техническая база знаний

Эта папка содержит автоматически создаваемую SQLite-базу `knowledge.sqlite3`. Её не нужно редактировать или добавлять в Git.

Ручные настройки аналогов конкурентов находятся в [`config/competitors.toml`](../../config/competitors.toml). Инструкция — в [`config/README.md`](../../config/README.md).
