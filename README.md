# MasterProfi AI Agent

Локальный агент формирует коммерческие предложения по ТЗ с использованием Python, Ollama и `qwen3.5:9b`.

## Запуск

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
./run_agent.command
```

Перед первым запуском положите локальный прайс в `Локальные данные/`, примеры ТЗ в `ПримерыТЗ/`, а старые КП для RAG и сверки — в `ПримерыКП/`. Эти каталоги содержат коммерческие и клиентские данные и намеренно не публикуются в Git. Готовое КП формируется непосредственно в PDF без Excel-шаблона.

Положите ТЗ в `input/`. Поддерживаются XLSX, DOCX и PDF. После успешного расчёта и проверки готовое PDF-КП появится в `output/`, а исходный файл будет удалён из `input/`. Если данных или ценового правила недостаточно, ТЗ останется на месте, а в `output/` появится JSON-отчёт.

## Что извлекается из ТЗ

- наименование позиции;
- ширина и высота либо площадь;
- количество.

Скидка, монтаж и доставка временно исключены из расчёта.

## Структура

- `source/main.py` — наблюдение за папкой `input`;
- `source/agent.py` — сценарий обработки одной заявки;
- `source/parser.py` — универсальный разбор XLSX/DOCX/PDF;
- `source/tools.py` — ценовые инструменты;
- `source/pdf_renderer.py` — формирование готового PDF-КП;
- `source/qwen.py` — клиент локальной Qwen;
- `source/knowledge.py` — SQLite/RAG, аналоги и источники конкурентов;
- `source/reviewer.py` — независимая проверка КП;
- `source/config/` — прокомментированные настройки TOML;
- `source/knowledge/` — редактируемая база знаний.

## Безопасность расчёта

RAG помогает понять структуру и найти подтверждённый аналог, но не является источником нашей цены. Итоговая цена должна иметь происхождение из локального прайса. Непроверенные аналоги и позиции без ценового правила блокируют выпуск КП.

## Тесты

```bash
.venv/bin/python -m source.tests.test_agent
```
